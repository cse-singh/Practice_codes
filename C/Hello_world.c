#include<conio.h>
// this tells the compiler to include the console input and output header file
#include <stdio.h>
// this tells the compiler to include the standard input and output header file

int main()
{
	printf("Hello World\n");
// 	prints Hello World on the user screen
	getch();
	// used to add a pause in execution for the user to be able to study the output
	// not necessary in newer IDEs
	
	return 0;
	// returns the value 0 to the OS
}